<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.kK-2004</groupId>
    <artifactId>kmessage-sdk</artifactId>
    <version>0.1.1</version>
    <packaging>jar</packaging>
    <name>kmessage-sdk</name>
    <description>Java client SDK for the kMessage multi-channel message delivery platform</description>
    <url>https://github.com/kK-2004/kMessage</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>kK-2004</name>
            <url>https://github.com/kK-2004</url>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/kK-2004/kMessage</url>
        <connection>scm:git:git://github.com/kK-2004/kMessage.git</connection>
        <developerConnection>scm:git:git@github.com:kK-2004/kMessage.git</developerConnection>
    </scm>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jackson.version>2.19.2</jackson.version>
        <junit.version>5.12.2</junit.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
            </plugin>
            <!-- 生成源码 jar，便于消费方查看源码 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- 发布到 GitHub Packages（未来 mvn deploy 使用，需在 settings.xml 配置 github 认证） -->
    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub kK-2004 Apache Maven Packages</name>
            <url>https://maven.pkg.github.com/kK-2004/kMessage</url>
        </repository>
    </distributionManagement>
</project>
